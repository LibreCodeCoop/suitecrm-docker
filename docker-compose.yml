version: '3.3'
services:
  nginx:
    image: nginx:latest
    restart: always
    ports:
      - 80:80
    depends_on:
      - php7
    volumes:
      - ./SuiteCRM:/app
      - ./.docker/nginx/conf/nginx.conf:/etc/nginx/conf.d/default.conf
  php7:
    build: ./.docker/php7
    restart: always
    volumes:
      - ./volumes/suitecrm:/app/SuiteCRM
      - ~/.composer:/root/.composer
    depends_on:
      - db
    environment:
      - SUITECRM_VERSION=v7.11.18
      - TZ=America/Sao_Paulo
    links:
      - db
  db:
    image: mariadb
    ports:
      - "3306:3306"
    volumes:
      -  ./.docker/mysql/conf:/etc/mysql/conf.d
      - ./volumes/mysql/dump:/docker-entrypoint-initdb.d
      - ./volumes/mysql/data:/var/lib/mysql
    environment:
      - MYSQL_ROOT_PASSWORD=root
      - MYSQL_DATABASE=root
